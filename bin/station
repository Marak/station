var Station = require('../lib/station').Station;

var station = new Station({
  name: "station",
  options: {
    "port": 9000,
    "basicAuth" : {
      "username": "admin",
      "password": "admin"
    }
  }
});

station.start();

